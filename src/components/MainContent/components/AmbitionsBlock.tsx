import React from "react";
import styled from "styled-components";

const Container = styled.div`
  height: 100vh;
  background-color: #61dafb;
  pointer-events: auto;
`;

export const AmbitionsBlock: React.FC = () => <Container>Ambitions</Container>;
